/target

# Machine-local: points worktree builds at a shared target-dir (absolute path)
/.cargo/config.toml

# Runtime state and generated output
/logs
/.feeds
*.log

# Nix / direnv
/.direnv
/.envrc

# Stray literal-tilde dir created when a `~` path isn't expanded
/~

# Ralph execution state
.ralph/
